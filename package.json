{
  "name": "hc-redux-middleware",
  "version": "2.0.0",
  "description": "Redux middleware for holochain rust",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && babel src --out-dir dist"
  },
  "author": "willem.olding@holo.host",
  "license": "AGPL-3.0",
  "dependencies": {
    "@holochain/hc-web-client": "file:../hc-web-client"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^23.6.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.6.0",
    "mock-socket": "^8.0.5",
    "simple-jsonrpc-js": "0.0.10"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
